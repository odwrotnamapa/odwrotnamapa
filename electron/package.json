{
  "name": "odwrotna-mapa-electron",
  "private": true,
  "version": "0.0.0",
  "description": "Electron platform project for Odwrotna Mapa.",
  "main": "build/main.js",
  "scripts": {
    "build": "tsc",
    "start": "electron .",
    "pack": "npm run build && capacitor-electron vendor && electron-builder --config electron-builder.config.js"
  },
  "devDependencies": {
    "electron": "^43.0.0",
    "electron-builder": "^26.0.12",
    "esbuild": "^0.28.1",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.4.2"
  }
}
